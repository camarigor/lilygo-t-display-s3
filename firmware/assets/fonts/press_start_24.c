/* PLACEHOLDER — substituir pelo .c gerado via LVGL Font Converter. */
#include "lvgl.h"

const lv_font_t press_start_24 = {
    .get_glyph_dsc = NULL,
    .get_glyph_bitmap = NULL,
    .line_height = 24,
    .base_line = 0,
    .subpx = LV_FONT_SUBPX_NONE,
    .underline_position = 0,
    .underline_thickness = 0,
    .static_bitmap = 0,
    .user_data = NULL,
};
